/**
 * Defines options on how tags presentations should be drawn.
 */
export interface ITagsUpdateOptions {
    /**
     * Defines whether the regions (color) background should be draw or made transparent.
     */
    showRegionBackground: boolean;
    /**
     * Defines whether the tags text be visible over the region.
     */
     showTagsText: boolean;
}
